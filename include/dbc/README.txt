Design by Contract for C++ (DbC++) header files.

DbC++ should be included only by #including "dbc.hpp" in parent directory. Do not include other header files separately.

This directory contains library API files.
The "detail" directory contains library implementation files.
